# ==========================
# FastAPI
# ==========================
torchvision==0.21.0
torch==2.6.0
fastapi==0.116.1
uvicorn[standard]==0.35.0

# ==========================
# Validation & Config
# ==========================

pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.1.1

# ==========================
# File Uploads
# ==========================

python-multipart==0.0.20
aiofiles==24.1.0

# ==========================
# Image Processing
# ==========================

Pillow==11.3.0
opencv-python==4.12.0.88

# ==========================
# AI / Diffusion
# ==========================
# torch is intentionally NOT listed here — install it separately first,
# with the CUDA build for your GPU:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Installing torch from PyPI directly (e.g. via this file) gives you the
# CPU-only build, which is what silently causes fallback to mock mode.

diffusers==0.35.1
transformers==4.56.0
accelerate==1.10.1
safetensors==0.6.2

# ==========================
# Face identity (IP-Adapter-FaceID) & restoration
# ==========================
# insightface needs onnxruntime to actually run its detection/embedding
# models. Use onnxruntime-gpu instead of onnxruntime if running on CUDA
# for meaningfully faster face detection (optional, CPU build works fine).
# Also powers the inswapper face-swap model used by "Generate Same Image"
# (app/services/face_swap.py) — no extra package needed, just its weights,
# which are auto-downloaded/cached on first use (see INSWAPPER_MODEL_URL
# in config.py).
insightface==0.7.3
onnxruntime==1.19.2

# GFPGAN face-restoration post-process pass. basicsr/facexlib are its
# runtime dependencies for loading/aligning faces.
gfpgan==1.3.8
basicsr==1.4.2
facexlib==0.3.0

# Real-ESRGAN post-process upscale pass (runs after GFPGAN face restoration).
# Reuses basicsr/facexlib above — no extra system deps needed beyond what's
# already installed for GFPGAN/insightface.
realesrgan==0.3.0

# ==========================
# Utilities
# ==========================

numpy==2.2.6
requests==2.32.4
tqdm==4.67.1

# ==========================
# Queue / Storage
# ==========================

celery==5.5.3
redis==6.4.0
pymongo==4.14.1
cloudinary==1.44.1

# ==========================
# Optional (Better Performance)
# ==========================

#xformers==0.0.31.post1
peft>=0.17.0